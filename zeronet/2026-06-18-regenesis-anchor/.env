# FALLBACK TASK (Phase E). Only needed if the Phase D activate-multiproof did NOT already
# initialize the ASR anchor to the genesis output root (Phase D sets it with block 0 directly and
# has no !=0 guard, so this is usually a no-op for regenesis). The !=0 block guard in
# FixStartingAnchorRoot.s.sol has been relaxed here for the genesis-0 anchor.

# TODO-CUTOVER: confirm base/contracts commit for the v1.1.0 ASR impl.
BASE_CONTRACTS_COMMIT=01dad230390cd69bcf130b5fc7a7a580b31650a7

GAME_TYPE=621

# TODO-CUTOVER: STARTING_ANCHOR_ROOT = new genesis output root (Phase C); block number = 0 (genesis).
STARTING_ANCHOR_ROOT=
STARTING_ANCHOR_L2_BLOCK_NUMBER=0

# PROXY_ADMIN_OWNER (SafeC) reused.
PROXY_ADMIN_OWNER=0x3d59999977e0896ee1f8783bb8251df16fb483e9
# TODO-CUTOVER: new L1 addresses from `op-deployer inspect l1` after live Phase B deploy.
L1_PROXY_ADMIN=
SYSTEM_CONFIG=
ANCHOR_STATE_REGISTRY_PROXY=
DISPUTE_GAME_FACTORY_PROXY=

RECORD_STATE_DIFF=true
