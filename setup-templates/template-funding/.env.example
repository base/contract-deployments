## Example .env for template-funding
# Do NOT commit real secrets or production .env files.

# Address that will sign and execute the funding transaction (must be Safe owner)
SENDER=REPLACE_WITH_SAFE_OWNER_ADDRESS

# Commit hash of Optimism repo for building / validation
OP_COMMIT=REPLACE_WITH_COMMIT_HASH

# Commit hash of Base contracts repo for building / validation
BASE_CONTRACTS_COMMIT=REPLACE_WITH_COMMIT_HASH

# Address of the Gnosis Safe that will fund the recipients
OWNER_SAFE=REPLACE_WITH_SAFE_ADDRESS

## Notes:
#  - SENDER must be a signer/owner of OWNER_SAFE
#  - Recipients and amounts are defined in funding.json
#  - Use recent commit hashes for OP_COMMIT and BASE_CONTRACTS_COMMIT
