## Example .env for template-gas-increase
# Do NOT commit real secrets or production .env files.

# Commit of the Optimism repo to use for building / validation
OP_COMMIT=REPLACE_WITH_COMMIT_HASH

# Commit of the Base contracts repo to use for building / validation
BASE_CONTRACTS_COMMIT=REPLACE_WITH_COMMIT_HASH

# Old and new gas limit values (integers)
OLD_GAS_LIMIT=REPLACE_WITH_OLD_VALUE
NEW_GAS_LIMIT=REPLACE_WITH_NEW_VALUE

# Other optional variables used by scripts (examples)
# OWNER_SAFE=0x...
# SAFE_NONCE=123

## Notes:
#  - Use a recent commit hash for OP_COMMIT and BASE_CONTRACTS_COMMIT.
#  - These are placeholders to help maintainers and contributors fill in the template.